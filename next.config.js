/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  optimizeFonts: false, // Disable font optimization to avoid build-time font fetching issues
};

module.exports = nextConfig;
